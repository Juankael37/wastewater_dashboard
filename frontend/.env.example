# Vite — copy to .env.development / .env.production (do not commit real keys)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# When the PWA calls the Worker instead of Flask:
# VITE_API_URL=https://wastewater-api.<your-subdomain>.workers.dev
